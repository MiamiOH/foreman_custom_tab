<table id="custom_table" class="<%= table_css_classes %>">
  <thead>
    <tr>
      <th colspan="2"><%= _('custom_tab') %></th>
    </tr>
  </thead>
  <tbody>
    <% custom_tab_fields(host).each do |name, value| %>
      <tr>
        <td><%= name %></td>
        <td><%= value %></td>
      </tr>
    <% end %>
  </tbody>
</table>
